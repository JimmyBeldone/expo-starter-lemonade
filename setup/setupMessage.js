const { icons, log, unicode } = require('./constants');

log(`

==========================================

${unicode(icons.rocket)}  Initializing Expo Stater Lemonade  ${unicode(
    icons.lemon,
)}

==========================================


${unicode(icons.packageIcon)} Installing dependencies ..

${unicode(icons.coffee)} Grab a cup of coffee... ${unicode(icons.coffee)}

`);
